import React from 'react';
import { mount } from 'enzyme';
import DailyForecastDetail from './dailyforecastdetails';
import { WeatherData } from '../__test_data__';

describe('DailyForecastDetails render', () => {
	/* const wrapper = mount(
		<DailyForecastDetail day={WeatherData.daily.data[0]} show="entered" />
	);

	test('Component renders', () => {
		expect(wrapper.exists()).toBe(true);
    }); */
	// Cannot test until Enzyme supports Hooks API

	test('', () => expect(true).toBe(true)); // Placeholder
});
